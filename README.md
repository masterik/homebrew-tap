# homebrew-tap

Homebrew tap for [mkit](https://github.com/masterik/mk-toolkit).

```
brew install masterik/tap/mkit
```

Formulas here are published automatically by GoReleaser on each `mk-toolkit` release —
don't hand-edit.
